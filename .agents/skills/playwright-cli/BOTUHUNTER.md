# BotHunter notes

Optional supplement to **BotHunter Recorder** (`tools/recorder`).

Use `playwright-cli` for quick interactive exploration when Recorder is overkill.
For HH Page Object work, prefer recorded `session.json` + `trace.zip` pipeline.

Do not store auth cookies or credentials in CLI state files committed to Git.
