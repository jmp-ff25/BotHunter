# BotHunter notes

- Test layout: `tests/unit/`, `tests/integration/`, `tests/bdd/`.
- Mock Playwright, SQLite, Telegram, AI at infrastructure boundaries.
- Run: `pytest -q` from repo root with venv active.
- BDD steps: `tests/bdd/steps/` — see `bothunter-bdd` skill.
