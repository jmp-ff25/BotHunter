# BotHunter notes

- Python 3.12; type hints on public functions, methods, port boundaries.
- Application ports: use `Protocol` or ABC in `src/bothunter/application/`.
- See also `.cursor/rules/code-quality.mdc`.
