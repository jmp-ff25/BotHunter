# BotHunter notes

- Prefer `tests/integration/` with mocked Playwright over e2e.
- HH UI exploration: use `bothunter-recorder` skill (`tools/recorder`), not ad-hoc scripts.
- Web/API binds loopback only (ADR-0001).
- Page Objects: `src/bothunter/infrastructure/hh/pages/`
